<nav class="navbar navbar-expand-lg bg-white fixed-top">
    <div class="container d-flex align-items-center justify-content-between">
        <a class="navbar-brand d-flex align-items-center"
           asp-area=""
           asp-controller="Home"
           asp-action="Index">
            <img src="/images/logo.svg" alt="Logo" height="30">
            <span class="ms-2 f-calvino">Sailing & People</span>
        </a>

        <button class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"ƒ
                data-bs-target="#navbarContent"
                aria-controls="navbarContent"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav mx-auto f-calvino">
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Home" asp-action="Index">@SharedResources.Home</a>
                </li>

                @if (ViewBag.Categories != null)
                {
                    @foreach (var category in ViewBag.Categories as IEnumerable<CategoryDto>)
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-action="Category" asp-route-id="@category.Id">
                                @category.LocalizedName
                            </a>
                        </li>
                    }
                }

                <li class="nav-item">
                    <a class="nav-link" asp-controller="Home" asp-action="Faq">@SharedResources.Faq</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" asp-controller="Home" asp-action="Contact">@SharedResources.Contact</a>
                </li>
            </ul>

            <div class="mt-3 d-flex align-items-center">
                <partial name="_CodeFilterFormPartial"  model="new CodeFilterViewModel()" />
            </div>
        </div>
    </div>
</nav>